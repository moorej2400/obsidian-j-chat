import { test } from "@playwright/test";

test.skip("Codex SDK authenticated chat can be exercised once local credentials are available", async () => {
  // Scaffold only: this should eventually launch the plugin provider path with
  // OPENAI_API_KEY/CODEX_API_KEY and a safe non-vault working directory.
});

